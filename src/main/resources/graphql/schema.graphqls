type Task {
    id: ID!
    title: String!
    description: String!
    completed: Boolean!
}

type Query {
    findAll: [Task]
}

type Mutation {
    createTask(title: String!, description: String!): Task!
}
